<div class="mx-auto max-w-4xl mt-10">
  <h1 class="text-4xl font-bold mb-6">Welcome to Marketer Gen</h1>
  <p class="text-lg text-gray-600 mb-8">AI-driven content generation platform for marketers</p>
  
  <% if authenticated? %>
    <div class="bg-green-50 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
      <p>You are logged in as: <%= Current.user.email_address %></p>
    </div>
    <div class="space-x-4">
      <%= button_to "Sign Out", session_path, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
    </div>
  <% else %>
    <div class="space-x-4">
      <%= link_to "Sign Up", sign_up_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded inline-block" %>
      <%= link_to "Sign In", new_session_path, class: "bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded inline-block" %>
    </div>
  <% end %>
</div>