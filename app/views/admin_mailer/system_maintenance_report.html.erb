<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .header { background-color: #f8f9fa; padding: 20px; border-bottom: 3px solid #007bff; }
    .content { padding: 20px; }
    .metrics { display: flex; flex-wrap: wrap; gap: 20px; margin: 20px 0; }
    .metric { background-color: #f8f9fa; padding: 15px; border-radius: 5px; flex: 1; min-width: 200px; }
    .metric h4 { margin: 0 0 10px 0; color: #007bff; }
    .metric .value { font-size: 24px; font-weight: bold; color: #28a745; }
    .footer { background-color: #f8f9fa; padding: 15px; border-top: 1px solid #dee2e6; font-size: 12px; color: #6c757d; }
    .alert { padding: 12px; border-radius: 4px; margin: 10px 0; }
    .alert-success { background-color: #d4edda; border: 1px solid #c3e6cb; color: #155724; }
    .alert-warning { background-color: #fff3cd; border: 1px solid #ffeaa7; color: #856404; }
  </style>
</head>
<body>
  <div class="header">
    <h1>🔧 System Maintenance Report</h1>
    <p>Automated maintenance completed on <%= @maintenance_time.strftime('%B %d, %Y at %I:%M %p') %></p>
  </div>

  <div class="content">
    <h2>Maintenance Summary</h2>
    
    <% if @maintenance_results.present? %>
      <div class="metrics">
        <% @maintenance_results.each do |operation, result| %>
          <div class="metric">
            <h4><%= operation.humanize %></h4>
            <div class="value"><%= result[:count] || result %></div>
            <% if result[:details] %>
              <p><%= result[:details] %></p>
            <% end %>
          </div>
        <% end %>
      </div>
      
      <div class="alert alert-success">
        <strong>✅ Maintenance Completed Successfully</strong><br>
        All requested cleanup operations have been completed without errors.
      </div>
    <% else %>
      <div class="alert alert-warning">
        <strong>⚠️ No Maintenance Operations Performed</strong><br>
        No maintenance operations were executed during this cycle.
      </div>
    <% end %>

    <h3>Next Recommended Actions</h3>
    <ul>
      <li>Monitor system performance for the next 24 hours</li>
      <li>Review any error logs for potential issues</li>
      <li>Schedule next maintenance window if needed</li>
      <li>Update backup verification procedures</li>
    </ul>

    <p>
      <strong>Maintenance performed by:</strong> <%= @admin_user.display_name %> (<%= @admin_user.email_address %>)<br>
      <strong>Timestamp:</strong> <%= @maintenance_time.strftime('%Y-%m-%d %H:%M:%S %Z') %>
    </p>
  </div>

  <div class="footer">
    <p>
      This is an automated system maintenance report. 
      For questions or concerns, please contact the system administrator.
    </p>
    <p>Generated by Marketer Gen Admin System</p>
  </div>
</body>
</html>