<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { background-color: #fff3cd; padding: 20px; border-radius: 5px; margin-bottom: 20px; }
      .content { background-color: #fff; padding: 20px; border: 1px solid #dee2e6; border-radius: 5px; }
      .changes { background-color: #f8d7da; border: 1px solid #f5c6cb; padding: 15px; border-radius: 5px; margin: 15px 0; }
      .btn { display: inline-block; padding: 10px 20px; background-color: #ffc107; color: #212529; text-decoration: none; border-radius: 5px; }
      .footer { margin-top: 20px; padding-top: 20px; border-top: 1px solid #dee2e6; font-size: 12px; color: #6c757d; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h2>✏️ Changes Requested</h2>
      </div>
      
      <div class="content">
        <p>Hello <%= @recipient.first_name %>,</p>
        
        <p>Changes have been requested for the following content:</p>
        
        <div class="changes">
          <h3><%= @content.title %></h3>
          <p><strong>Campaign:</strong> <%= @campaign.name %></p>
          <p><strong>Content Type:</strong> <%= @content.content_type.humanize %></p>
          <p><strong>Requested by:</strong> <%= @requester&.full_name || 'Unknown' %></p>
          <% if @requested_changes.present? %>
            <p><strong>Requested Changes:</strong></p>
            <p><%= simple_format(@requested_changes) %></p>
          <% end %>
        </div>
        
        <p>Please review and make the necessary changes, then resubmit for approval.</p>
        
        <p><a href="<%= @content_url %>" class="btn">Edit Content</a></p>
      </div>
      
      <div class="footer">
        <p>This is an automated notification from MarketerGen.</p>
        <p>Please do not reply to this email.</p>
      </div>
    </div>
  </body>
</html>