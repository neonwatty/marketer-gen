<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { background-color: #f8d7da; padding: 20px; border-radius: 5px; margin-bottom: 20px; border: 1px solid #f5c6cb; }
      .content { background-color: white; padding: 20px; border: 1px solid #e9ecef; border-radius: 5px; }
      .button { display: inline-block; padding: 10px 20px; background-color: #007bff; color: white; text-decoration: none; border-radius: 5px; margin: 10px 5px; }
      .button.approve { background-color: #28a745; }
      .button.reject { background-color: #dc3545; }
      .urgent { color: #dc3545; font-weight: bold; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1 class="urgent">⏰ URGENT: Deadline Warning</h1>
        <h2><%= @content.title %></h2>
        <p class="urgent">Only <%= @hours_remaining %> hours remaining!</p>
      </div>
      
      <div class="content">
        <p>Hello <%= @approver.full_name %>,</p>
        
        <p><strong>URGENT:</strong> The approval deadline for the following content is approaching fast:</p>
        
        <ul>
          <li><strong>Title:</strong> <%= @content.title %></li>
          <li><strong>Content Type:</strong> <%= @content.content_type.titleize %></li>
          <li><strong>Campaign:</strong> <%= @campaign.name %></li>
          <li><strong>Created by:</strong> <%= @content.created_by.full_name %></li>
          <li class="urgent"><strong>Time Remaining:</strong> <%= @hours_remaining %> hours</li>
        </ul>
        
        <p><strong>Please review and approve or reject this content immediately to avoid delays:</strong></p>
        
        <div style="text-align: center; margin: 20px 0;">
          <a href="<%= @approval_url %>" class="button approve">Approve Now</a>
          <a href="<%= @reject_url %>" class="button reject">Reject</a>
        </div>
        
        <p>Time is critical - your immediate attention is required!</p>
        
        <p>Best regards,<br>MarketerGen Team</p>
      </div>
    </div>
  </body>
</html>