<%= rails_admin_form_for @object, url: unsuspend_path(model_name: @abstract_model.to_param, id: @object.id), as: @abstract_model.param_key, html: { method: :post } do |form| %>
  <div class="form-group">
    <h3>Unsuspend User: <%= @object.email_address %></h3>
    
    <div class="alert alert-info">
      <strong>Current Suspension Details:</strong>
      <ul>
        <li>Suspended at: <%= @object.suspended_at.strftime("%B %d, %Y at %I:%M %p") %></li>
        <li>Suspended by: <%= @object.suspended_by&.email_address || "System" %></li>
        <li>Reason: <%= @object.suspension_reason %></li>
      </ul>
    </div>
    
    <p>Are you sure you want to unsuspend this user? They will be able to log in immediately after unsuspension.</p>
  </div>
  
  <div class="form-actions">
    <button type="submit" class="btn btn-success">
      <i class="fa fa-check-circle"></i> Unsuspend User
    </button>
    <%= link_to "Cancel", back_or_index, class: "btn btn-default" %>
  </div>
<% end %>